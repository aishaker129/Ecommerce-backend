version: "3.8"

services:

  app:
    build: .
    container_name: campuskart-app
    ports:
      - "8081:8081"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: campuskart-redis
    ports:
      - "6379:6379"